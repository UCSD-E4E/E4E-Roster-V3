{% extends "layout.njk" %}
{% block title %}Dashboard — E4E Roster{% endblock %}
{% block body %}
<main class="app-main">
  <h2>Welcome, {{ user.name or user.username }}</h2>
  <p class="text-muted">{{ user.email }}</p>

  <div class="card-grid">
    <div class="card">
      <h3>Account Status</h3>
      <p class="text-muted">Check your account across E4E systems.</p>
      <a href="/account" class="btn btn-secondary">View Status</a>
    </div>

    {% if user.isAdmin %}
    <div class="card">
      <h3>Admin</h3>
      <p class="text-muted">Manage roster, provision accounts, view audit log.</p>
      <a href="/admin/users" class="btn btn-secondary">Open Admin</a>
    </div>
    {% endif %}

    {% if user.isSystemAdmin %}
    <div class="card">
      <h3>System</h3>
      <p class="text-muted">Manage organisations, LDAP group mappings, and system admin groups.</p>
      <a href="/system/orgs" class="btn btn-secondary">System Admin</a>
    </div>
    {% endif %}
  </div>
</main>
{% endblock %}
